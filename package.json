{
  "name": "football-plays",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute football-plays --local --file=schema.sql",
    "db:remote": "wrangler d1 execute football-plays --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "4.122.0"
  }
}
